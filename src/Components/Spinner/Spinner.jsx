import { SpinnerOverlay, SpinnerContainer } from "./Spinner";

const Spinner = () => {
    return(
        <SpinnerOverlay>
            <SpinnerContainer/>
        </SpinnerOverlay>
    )
}

export default Spinner
